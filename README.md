## 🧱 Блок-схема уровней
```
                ┌──────────────────────────┐
                │       Админ-панель       │
                │ (загрузка фото с Я.Диска)│
                └────────────┬─────────────┘
                             │
                             ▼
         ┌────────────────────────────────────────┐
         │  ETL + Face Recognition Pipeline       │
         │ - Детекция и выравнивание лиц          │
         │ - Извлечение эмбеддингов (DeepFace)    │
         │ - Кластеризация и присвоение user_id   │
         │ - Сохранение (user_id → photo_ids)     │
         └────────────────┬───────────────────────┘
                          │
                          ▼
          ┌─────────────────────────────────┐
          │       Database / Storage        │
          │ - users: user_id, mean_embedding│
          │ - photos: photo_id, path        │
          │ - mapping: user_id ↔ photo_ids  │
          └────────────────┬────────────────┘
                           │
                           ▼
              ┌────────────────────────────┐
              │ Telegram Bot Interface     │
              │ - принимает фото           │
              │ - ищет user_id по embedding│
              │ - возвращает фото          │
              └────────────────────────────┘

```

# 🧩 Общий пайплайн системы "FaceFinder"
## 1️⃣ Загрузка и первичная обработка фото (админ-зона)

**Цель:** загрузить пачку фото с мероприятий, распознать всех людей и распределить их по ID-пользователям.

### ⚙️ Шаги:

1. **Загрузка исходных фото**
   - Админ загружает фотографии с Яндекс.Диска или любого внешнего источника.
   - Скрипт скачивает все файлы в директорию:
     ```
     /srv/project/photos/raw_uploads/
     ```

**2. Обработка фото:**

Для каждого изображения:
- детектируются лица (`MTCNN` / `RetinaFace`);
- производится face alignment (2D/3D выравнивание);
- извлекается эмбеддинг (`ArcFace` или `AdaFace`, через `InsightFace` / `DeepFace`);
- определяется угол поворота головы (pose);
- если лицо сильно повернуто → либо пропускаем, либо используем 3D-реконструкцию.

**3. Сопоставление лиц с уже существующими пользователями:**

- эмбеддинг сравнивается с существующими `mean_embedding` в БД (через FAISS или cosine distance);
- если расстояние < threshold → лицо считается уже известным, добавляется под существующий `user_id`;
- если нет совпадений → создаётся новый `user_id`.

**4. Усреднение эмбеддингов для каждого пользователя (multi-view embedding):**

- при добавлении нового фото обновляется средний вектор лица пользователя.

**5. Сохранение данных:**

- фото сохраняется в /srv/project/photos/{user_id}/{photo_id}.jpg;

в БД добавляется запись:

Таблица photos
| photo_id | file_path | upload_date | meta (pose, quality) |

Таблица users
| user_id | mean_embedding | created_at |

Таблица user_photos
| user_id | photo_id |

2️⃣ База данных и индексы

Цель: обеспечить быстрый поиск по лицам и доступ к фото.

⚙️ Состав:

PostgreSQL — метаданные, связи и пути.

FAISS — быстрый поиск ближайших эмбеддингов (millions-scale).

Структура хранения на сервере:

/srv/project/photos/
├── 0001/
│   ├── 1.jpg
│   ├── 2.jpg
├── 0002/
│   ├── 5.jpg
│   └── 6.jpg
└── ...

💾 Содержание таблиц:

users
| user_id | mean_embedding (vector) | created_at |

photos
| photo_id | file_path | upload_date | meta (pose, quality) |

user_photos
| user_id | photo_id |

3️⃣ Telegram-бот: поиск по лицу

Цель: пользователь загружает фото → получает все фото, где он есть.

⚙️ Шаги:

Пользователь отправляет фото в бот.

Бот сохраняет фото во временную директорию /srv/project/temp/.

ML-модуль:

извлекает лицо → выравнивает → получает эмбеддинг;

ищет ближайший mean_embedding в FAISS / БД;

если найдено совпадение < threshold → получает user_id.

Бот достаёт все photo_id из user_photos для этого user_id.

По photo_id получает пути из photos.file_path.

Отправляет пользователю все найденные фото (по 10 штук за раз, с пагинацией).

4️⃣ Механизмы устойчивости и качества

✅ Выравнивание (Alignment) — вращаем лицо в фронтальный вид перед эмбеддингом.
✅ Multi-view Embeddings — усредняем вектор по всем фото пользователя.
✅ Pose filtering — при загрузке игнорируем фото с поворотом > 70°.
✅ Гибкий threshold — если совпадение неуверенное, бот может показать «возможные совпадения».
✅ Кэширование — эмбеддинги и пути пользователей хранятся в Redis, чтобы ускорить поиск.

5️⃣ Админ-панель

Просмотр и валидация автоматически созданных пользователей (объединение/разделение).

Удаление дублей (если модель ошиблась и создала двух похожих пользователей).

Просмотр метаданных по качеству распознавания.
